# 튜플
소괄호() 형태로 이루어짐.

# 가상환경 설정
python -m venv venv  
.venv\Scripts\activate

# PaddleOCR
## Local Python Library/n
Runs PaddleOCR pipelines directly on the local machine. This mode requires a suitable local environment and hardware, and is ideal for offline use or privacy-sensitive scenarios.

